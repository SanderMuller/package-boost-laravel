<?php declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | PackageBoostLaravel configuration
    |--------------------------------------------------------------------------
    |
    | Add your package's configuration keys here. Consumers publish this file
    | via `php artisan vendor:publish --tag=package-boost-laravel-config`.
    |
    */
];
